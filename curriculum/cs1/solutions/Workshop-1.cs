using System;

namespace solutions {
  public class Workshop1 {
    /*
     * 1.1. (sum2)
     * takes in 2 numbers, and returns their sum.
     */
    public int Add(int a, int b) {
      return a + b;
    }
  }
}
